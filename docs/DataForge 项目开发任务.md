### **DataForge 项目开发任务

#### **阶段一：核心基础设施与项目骨架**

1. **项目初始化与基础结构**
    
    - 任务-1.1.1: 创建项目仓库并初始化Git。
        
    - 任务-1.1.2: 设置Python虚拟环境。
        
    - 任务-1.1.3: 定义 `pyproject.toml` (或 `setup.py`)，管理项目元数据和基本依赖。
        
    - 任务-1.1.4: 添加 `.gitignore` 和 `README.md`。
        
2. **依赖管理与基础工具**
    
    - 任务-1.2.1: 添加 `Click` 库到依赖，并验证基本命令行功能。
        
    - 任务-1.2.2: 添加 `PyYAML` 和 `json` 库到依赖。
        
    - 任务-1.2.3: 添加 `Pydantic` 库到依赖。
        
    - 任务-1.2.4: 添加 `Pandas` 库到依赖 (用于参考数据)。
        
3. **日志与错误处理**
    
    - 任务-1.3.1: 配置标准Python `logging` 模块，支持控制台和文件输出。
        
    - 任务-1.3.2: 定义项目统一的异常类结构。
        
    - 任务-1.3.3: 实现全局异常捕获和友好错误信息输出。
        
4. **配置管理模块 (基础)**
    
    - 任务-1.4.1: 实现命令行参数解析器 (`Click` 或 `argparse`) 的骨架。
        
    - 任务-1.4.2: 实现从指定YAML/JSON文件加载配置的基础功能。
        
    - 任务-1.4.3: 使用 `Pydantic` 为通用配置（如输出格式、数量）定义数据模型和校验。
        
    - 任务-1.4.4: 实现配置合并逻辑（CLI参数覆盖文件配置）。
        

#### **阶段二：核心数据生成引擎**

1. **数据生成器抽象与工厂**
    
    - 任务-2.1.1: 定义 `DataGenerator` 抽象基类 (`abc.ABC`)，包含 `type_name`, `generate`, `validate` 抽象方法。
        
    - 任务-2.1.2: 实现 `GeneratorFactory` 类，负责注册和获取 `DataGenerator` 实例。
        
    - 任务-2.1.3: 实现核心随机数生成工具类 (如 `random.SystemRandom` 或 `secrets`)。
        
2. **数据校验模块 (基础)**
    
    - 任务-2.2.1: 在 `DataGenerator` 抽象类中定义 `validate` 方法。
        
    - 任务-2.2.2: 实现一个通用的校验报告收集器。
        
3. **数据输出模块 (基础)**
    
    - 任务-2.3.1: 定义 `DataFormatter` 抽象类（如 `format_record` 方法）。
        
    - 任务-2.3.2: 实现 `CSVFormatter` (支持标题行、引用)。
        
    - 任务-2.3.3: 实现 `JSONFormatter` (支持单行/美化输出)。
        
    - 任务-2.3.4: 定义 `DataExporter` 抽象类（如 `export_data` 方法）。
        
    - 任务-2.3.5: 实现 `FileWriter` (将数据写入文件)。
        
    - 任务-2.3.6: 实现 `StdoutWriter` (将数据输出到标准输出)。
        
    - 任务-2.3.7: 确保输出模块支持流式写入，避免内存溢出。
        

#### **阶段三：基础信息类数据生成器**

1. **通用数值生成器**
    
    - 任务-3.1.1: 实现 `IntegerGenerator` (范围、正负、零)。
        
    - 任务-3.1.2: 实现 `DecimalGenerator` (范围、精度、正负、零)。
        
    - 任务-3.1.3: 实现 `RandomNumberGenerator` (指定长度、前导零)。
        
2. **通用文本生成器**
    
    - 任务-3.2.1: 实现 `StringGenerator` (长度范围、字符集)。
        
    - 任务-3.2.2: 实现 `BooleanGenerator` (格式、比例)。
        
    - 任务-3.2.3: 实现 `EnumGenerator` (自定义值列表、权重)。
        
3. **基础信息类 (实现并集成校验和CLI参数)**
    
    - 任务-3.3.1: **姓名 (Name)**
        
        - 实现 `NameGenerator` (中文/英文，性别，随机组合)。
            
        - 准备中文姓氏/名字、英文名/姓氏静态数据文件。
            
        - 集成到 `GeneratorFactory`。
            
        - 添加CLI参数支持 (`--name.type`, `--name.gender` 等)。
            
    - 任务-3.3.2: **手机号码 (Phone Number)**
        
        - 实现 `PhoneNumberGenerator` (中国大陆号段，长度11位)。
            
        - 添加基本校验逻辑。
            
        - 添加CLI参数支持 (`--phone.region`, `--phone.prefix`, `--phone.valid` 等)。
            
    - 任务-3.3.3: **银行卡号/信用卡号 (Bank Card / Credit Card Number)**
        
        - 实现 `BankCardGenerator` (Luhn算法，BIN码模拟)。
            
        - 准备BIN码静态数据文件。
            
        - 添加校验逻辑。
            
        - 添加CLI参数支持 (`--bankcard.type`, `--bankcard.issuer`, `--bankcard.valid` 等)。
            
    - 任务-3.3.4: **身份证号码 (ID Card Number)**
        
        - 实现 `IDCardGenerator` (地区代码、出生日期、顺序码、校验位)。
            
        - 准备最新行政区划代码静态数据文件。
            
        - 添加GB32100-2015校验逻辑。
            
        - 添加CLI参数支持 (`--idcard.region`, `--idcard.birth_date_range`, `--idcard.gender`, `--idcard.valid` 等)。
            
    - 任务-3.3.5: **年龄 (Age)**
        
        - 实现 `AgeGenerator` (范围、分布)。
            
        - 添加CLI参数支持 (`--age.min`, `--age.max`, `--age.distribution` 等)。
            
    - 任务-3.3.6: **邮箱 (Email)**
        
        - 实现 `EmailGenerator` (用户名、域名)。
            
        - 添加基本校验逻辑。
            
        - 添加CLI参数支持 (`--email.domains`, `--email.username_length`, `--email.valid` 等)。
            
    - 任务-3.3.7: **性别 (Gender)**
        
        - 实现 `GenderGenerator` (男/女/其他，权重)。
            
        - 添加CLI参数支持 (`--gender.type`, `--gender.male_ratio` 等)。
            

#### **阶段四：高级数据类型生成器 (迭代开发)**

**注**: 以下任务将按照文档中列出的顺序，逐个数据类型进行实现和集成。每个任务都包含：

- 实现对应的 `DataGenerator` 子类。
    
- 编写其核心生成逻辑。
    
- 实现其 `validate` 方法（如果适用）。
    
- 准备所需的静态参考数据（如果需要）。
    
- 集成到 `GeneratorFactory`。
    
- 为该生成器添加所有建议的CLI配置参数支持。
    

1. **标识类**
    
    - 任务-4.1.1: 全局唯一ID (UUID/ULID/Snowflake)
        
    - 任务-4.1.2: 业务单据号 (订单号、发票号等)
        
    - 任务-4.1.3: 产品编码 (SKU, EAN/UPC, ISBN, ISSN)
        
    - 任务-4.1.4: 社保／医保号
        
    - 任务-4.1.5: 护照号、签证号、驾驶证号
        
    - 任务-4.1.6: 优惠券码／促销码
        
    - 任务-4.1.7: 物流单号／运单号
        
2. **联系／通信类**
    
    - 任务-4.2.1: 电子邮件验证码
        
    - 任务-4.2.2: 短信验证码
        
    - 任务-4.2.3: 传真号码
        
    - 任务-4.2.4: 坐机固话
        
    - 任务-4.2.5: URL／URI
        
    - 任务-4.2.6: 文件路径
        
    - 任务-4.2.7: MIME Type
        
3. **网络／设备类**
    
    - 任务-4.3.1: IP 地址 (IPv4/IPv6)
        
    - 任务-4.3.2: 域名
        
    - 任务-4.3.3: MAC 地址
        
    - 任务-4.3.4: 端口号
        
    - 任务-4.3.5: HTTP 头
        
    - 任务-4.3.6: Session ID／Token
        
    - 任务-4.3.7: 设备 ID／IMEI／IMSI
        
    - 任务-4.3.8: 地理坐标
        
    - 任务-4.3.9: 时区标识
        
4. **文本／多语言类**
    
    - 任务-4.4.1: 长文本
        
    - 任务-4.4.2: 富文本／HTML、Markdown 片段
        
    - 任务-4.4.3: Unicode 边界字符
        
    - 任务-4.4.4: 多语言示例
        
    - 任务-4.4.5: 特殊字符
        
    - 任务-4.4.6: 用户行为数据
        
5. **结构化／半结构化数据**
    
    - 任务-4.5.1: JSON 对象／数组 (基于Schema/模板)
        
    - 任务-4.5.2: XML 文档 (基于Schema/模板)
        
    - 任务-4.5.3: YAML (基于Schema/模板)
        
    - 任务-4.5.4: CSV／TSV (列定义、分隔符)
        
    - 任务-4.5.5: 表单数据 (URL编码/Multipart)
        
6. **数值／计量类**
    
    - 任务-4.6.1: 小数
        
    - 任务-4.6.2: 整数
        
    - 任务-4.6.3: 负数
        
    - 任务-4.6.4: 币种
        
    - 任务-4.6.5: 科学计数法
        
    - 任务-4.6.6: 高精度小数
        
    - 任务-4.6.7: 度量单位组合
        
    - 任务-4.6.8: 百分比、利率、汇率
        
    - 任务-4.6.9: 坐标／矩阵数据
        
    - 任务-4.6.10: 颜色值
        
7. **时间／日历类**
    
    - 任务-4.7.1: 日期
        
    - 任务-4.7.2: 时间
        
    - 任务-4.7.3: 时间戳
        
    - 任务-4.7.4: ISO 8601 周日期
        
    - 任务-4.7.5: 年份区间
        
    - 任务-4.7.6: 时段／持续时间
        
    - 任务-4.7.7: 金融交易日历
        
    - 任务-4.7.8: Cron 表达式
        
8. **安全／注入测试数据**
    
    - 任务-4.8.1: SQL 注入 payload
        
    - 任务-4.8.2: XSS 攻击脚本
        
    - 任务-4.8.3: 命令注入
        
    - 任务-4.8.4: 路径穿越
        
    - 任务-4.8.5: 二进制／Base64 编码数据
        
9. **媒体／二进制及文件相关类**
    
    - 任务-4.9.1: 图像文件头
        
    - 任务-4.9.2: 音频片段
        
    - 任务-4.9.3: 视频片段
        
    - 任务-4.9.4: 压缩包
        
    - 任务-4.9.5: PDF／Office 文档
        
    - 任务-4.9.6: 图片/视频文件（模拟）
        
    - 任务-4.9.7: 文件扩展名
        
    - 任务-4.9.8: 文件大小
        
    - 任务-4.9.9: 图片尺寸
        
10. **枚举／状态码**
    
    - 任务-4.10.1: HTTP 状态码
        
    - 任务-4.10.2: 自定义业务状态码
        
    - 任务-4.10.3: 布尔型
        
    - 任务-4.10.4: 枚举
        
    - 任务-4.10.5: 优先级
        
11. **特殊场景数据（通用）**
    
    - 任务-4.11.1: 空值/Null值
        
    - 任务-4.11.2: 边界值/极端值
        
    - 任务-4.11.3: 非法/异常数据
        
    - 任务-4.11.4: 可自定义长度格式的业务编号
        
    - 任务-4.11.5: 重复数据
        
    - 任务-4.11.6: 排序数据
        
    - 任务-4.11.7: 并发/竞争数据 (模拟逻辑)
        

#### **阶段五：Web API 服务 (可选，如果需要)**

1. **FastAPI 服务骨架**
    
    - 任务-5.1.1: 初始化 FastAPI 应用。
        
    - 任务-5.1.2: 添加健康检查 `/health` 端点。
        
    - 任务-5.1.3: 实现 `/data_types` 端点，列出所有支持的数据类型及其参数Schema。
        
    - 任务-5.1.4: 配置CORS。
        
2. **数据生成 API 端点**
    
    - 任务-5.2.1: 实现 `POST /generate/{data_type}` 端点，接收Pydantic请求模型。
        
    - 任务-5.2.2: 将API请求转发到核心编排器。
        
    - 任务-5.2.3: 定义API响应模型。
        
    - 任务-5.2.4: 实现API的错误处理和统一响应格式。
        
3. **异步任务处理 (针对大规模生成)**
    
    - 任务-5.3.1: 调研 `Celery` 或 `Dramatiq` 等异步任务队列。
        
    - 任务-5.3.2: 集成选定的任务队列 (如 `Celery` + `Redis` 作为Broker)。
        
    - 任务-5.3.3: 将大规模数据生成任务封装为异步任务。
        
    - 任务-5.3.4: 实现任务状态查询API (如 `GET /tasks/{task_id}/status`)。
        

#### **阶段六：高级功能与部署**

1. **数据关联性与上下文管理**
    
    - 任务-6.1.1: 设计并实现 `GenerationContext` 类，用于在生成过程中传递和存储已生成的数据。
        
    - 任务-6.1.2: 实现生成器间的依赖管理和拓扑排序（如果存在复杂循环依赖，需要警告或处理）。
        
    - 任务-6.1.3: 调整相关生成器（如身份证与年龄、姓名与邮箱）以利用 `GenerationContext`。
        
2. **扩展机制 (完整实现)**
    
    - 任务-6.2.1: 实现 `DataForge` 启动时扫描和加载 `setuptools` entry points 的逻辑。
        
    - 任务-6.2.2: 编写一个示例自定义生成器插件，并验证其可被 `DataForge` 发现和使用。
        
3. **参考数据管理 (优化)**
    
    - 任务-6.3.1: 优化静态数据加载性能 (如使用 `feather` 或 `parquet` 格式)。
        
    - 任务-6.3.2: 实现参考数据文件的自动更新机制 (如果需要，如从URL下载)。
        
4. **DevOps 与部署**
    
    - 任务-6.4.1: 编写 `Dockerfile` 用于打包 `DataForge` 应用。
        
    - 任务-6.4.2: 编写 `docker-compose.yaml` 用于本地快速启动CLI和API服务。
        
    - 任务-6.4.3: 配置 GitHub Actions (或 GitLab CI) 实现自动化测试、Docker镜像构建和推送到Registry。
        
    - 任务-6.4.4: 编写基础的 Kubernetes 部署文件 (Deployment, Service) (如果选择K8s)。
        

#### **阶段七：测试与文档**

1. **测试用例编写**
    
    - 任务-7.1.1: 为每个 `DataGenerator` 编写单元测试，覆盖生成逻辑和校验逻辑。
        
    - 任务-7.1.2: 编写集成测试，验证CLI命令和API端点的端到端功能。
        
    - 任务-7.1.3: 编写性能测试脚本，评估生成速度和资源消耗。
        
    - 任务-7.1.4: 编写数据真实性/准确性验证脚本，对关键数据类型（如身份证、银行卡）进行批量验证。
        
2. **文档编写**
    
    - 任务-7.2.1: 撰写详细的 `README.md`，包含安装、快速开始、CLI用法。
        
    - 任务-7.2.2: 编写配置文件示例和JSON Schema示例。
        
    - 任务-7.2.3: 撰写扩展机制的开发者文档。
        
    - 任务-7.2.4: 基于FastAPI自动生成的OpenAPI文档进行补充和完善。